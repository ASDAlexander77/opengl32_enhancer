; Fixture config for texture_mipmap_gpu_test.cpp - copied to that test's own build directory as
; opengl32_enhancer.ini (see CMakeLists.txt), NOT the project's real shipped ini. The feature
; defaults to off, so the test needs a file that turns it on; a fixture rather than the shipped
; ini so the test's expectations don't move if the shipped default ever changes.
autoMipmap=1
