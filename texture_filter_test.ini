; Fixture config for texture_filter_test.cpp - copied to that test's own build directory as
; opengl32_enhancer.ini (see CMakeLists.txt), NOT the project's real shipped ini. A fixed,
; known anisotropy value so the test's expectations don't drift if the shipped ini's value
; ever changes.
anisotropy=8
